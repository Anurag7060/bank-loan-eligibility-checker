services:
  portal:
    build: .
    ports:
      - "8080:8080"
    environment:
      HOST: 0.0.0.0
      PORT: 8080
      DATABASE_PATH: /app/data/loan_portal.db
    env_file:
      - path: .env
        required: false
    volumes:
      - loan_portal_data:/app/data

volumes:
  loan_portal_data:
